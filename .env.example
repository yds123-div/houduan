# Server
PORT=3000
NODE_ENV=development

# MongoDB（对应 D:\docker-infra 里的 shared-mongo 容器）
MONGO_URL="mongodb://<user>:<password>@<host>:<port>/<dbname>?authSource=admin"

# JWT（签名与校验 token 的密钥，生产环境务必用长随机字符串）
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1d
